language: python
python:
    - "3.4"
install: 
    - "pip install -r requirements.txt"
    - "pip install coverage"
    - "pip install coveralls"
script: 
    - "coverage run --source=krobot test"
after_success:
    coveralls